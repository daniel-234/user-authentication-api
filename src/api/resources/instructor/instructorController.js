import { generateControllers } from '../../modules/query.js';

// Declare the Instructor Model as an empty object until we
// define a Model.
const Instructor = {};

export default generateControllers(Instructor);
